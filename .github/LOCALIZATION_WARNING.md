# WARNING: Localization Files Sync

Do not modify `locales/*.yml` files directly unless explicitly confirmed by the user. 
These files are synchronized with Codeberg Weblate for community translations. Any direct modifications to these files locally will cause merge conflicts with the upstream translation memory.

If translation changes are required, warn the user first and explicitly ask for permission before proceeding.
